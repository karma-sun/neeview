<Window x:Class="NeeView.ExportImageWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:NeeView"
        xmlns:properties="clr-namespace:NeeView.Properties" mc:Ignorable="d" Style="{StaticResource DefaultWindowStyle}" Foreground="{DynamicResource NVForeground}"
        Background="{DynamicResource NVBackground}" Title="{x:Static properties:Resources.ExportImageWindow_Title}" Height="600" Width="800" ResizeMode="CanResizeWithGrip">

    <Window.Resources>

        <Style x:Key="TextButton" TargetType="Button" BasedOn="{StaticResource NVDialogButtonStyle}">
            <Setter Property="Margin" Value="10,10,0,0" />
            <Setter Property="MaxWidth" Value="512" />
        </Style>

        <Style x:Key="TextButtonMain" TargetType="Button" BasedOn="{StaticResource TextButton}">
            <Setter Property="Foreground" Value="{StaticResource NVDialogButtonForeground}" />
            <Setter Property="Background" Value="{StaticResource NVDialogButtonBackground}" />
        </Style>

        <Style x:Key="ItemStack" TargetType="StackPanel">
            <Setter Property="Margin" Value="0,5" />
        </Style>

        <Style x:Key="Header" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>

        <Style x:Key="Body" TargetType="TextBlock">
            <Setter Property="Background" Value="Transparent" />
        </Style>

        <Style x:Key="BodyCheckBox" TargetType="CheckBox">
            <Setter Property="FocusVisualStyle" Value="{StaticResource NVBorderFocusVisual}" />
            <Setter Property="Foreground" Value="{DynamicResource NVForeground}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="BodyComboBox" TargetType="ComboBox" BasedOn="{StaticResource NVComboBox}">
            <Setter Property="Foreground" Value="{StaticResource NVButtonForeground}" />
        </Style>

    </Window.Resources>

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" MinWidth="{Binding ElementName=Buttons, Path=ActualWidth}" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <DockPanel>
            <StackPanel x:Name="Buttons" DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                <Button x:Name="SaveButton" Style="{StaticResource TextButtonMain}" Content="{x:Static properties:Resources.ExportImageWindow_SaveAs}" Click="SaveButton_Click" />
                <Button x:Name="CancelButton" Style="{StaticResource TextButton}" Content="{x:Static properties:Resources.Word_Cancel}" Click="CancelButton_Click" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Margin="10">
                <StackPanel Style="{StaticResource ItemStack}">
                    <TextBlock Style="{StaticResource Header}" Text="{x:Static properties:Resources.ExportImageWindow_Mode}" />
                    <ComboBox Style="{StaticResource BodyComboBox}" ItemsSource="{Binding ExportImageModeList}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding Mode}"
                            TabIndex="1" />
                    <CheckBox Content="{x:Static properties:Resources.ExportImageWindow_InclideBackground}" IsChecked="{Binding HasBackground}" Margin="20,10,0,0" TabIndex="1"
                            FocusVisualStyle="{StaticResource NVBorderFocusVisual}">
                        <CheckBox.Style>
                            <Style TargetType="CheckBox" BasedOn="{StaticResource BodyCheckBox}">
                                <Setter Property="Visibility" Value="Hidden" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Mode}" Value="{x:Static local:ExportImageMode.View}">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </CheckBox.Style>
                    </CheckBox>
                </StackPanel>
            </StackPanel>

            <StackPanel DockPanel.Dock="Bottom" VerticalAlignment="Bottom" Margin="10,0">

                <StackPanel Style="{StaticResource ItemStack}">
                    <TextBlock Style="{StaticResource Header}" Text="{x:Static properties:Resources.ExportImageWindow_Folder}" />
                    <DockPanel>
                        <Button x:Name="DestinationFolderOptionButton" DockPanel.Dock="Right" Style="{StaticResource IconButton}" Click="DestinationFolderOptionButton_Click" TabIndex="102">
                            <Image Source="{StaticResource ic_setting_24px_b}" Width="16" Height="16" />
                        </Button>
                    <ComboBox Style="{StaticResource BodyComboBox}" ItemsSource="{Binding DestinationFolderList}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedDestinationFolder}"
                            TabIndex="101"/>
                    </DockPanel>
                </StackPanel>

            </StackPanel>
        </DockPanel>

        <Grid x:Name="Base" Grid.Column="1" Background="#EEE">
            <DockPanel Margin="20">
                <TextBlock DockPanel.Dock="Bottom" Text="{Binding ImageFormatNote}" Margin="10" HorizontalAlignment="Center" Foreground="Gray" />
                <Viewbox Margin="20" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ContentControl Content="{Binding Preview}" HorizontalAlignment="Center" VerticalAlignment="Center" Focusable="False" IsTabStop="False" />
                </Viewbox>
            </DockPanel>
        </Grid>

        <GridSplitter Grid.Column="1" Width="8" HorizontalAlignment="Left" Background="Transparent" FocusVisualStyle="{StaticResource NVFocusVisual}" IsTabStop="False" />

    </Grid>
</Window>
