<UserControl x:Class="NeeView.MainViewBay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeeView"
             mc:Ignorable="d"
             IsHitTestVisible="True" Background="{Binding ThemeBrush.IconBackgroundBrush}" Foreground="{Binding ThemeBrush.ForegroundBrush}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!--
        <Grid Width="6" Background="{Binding ThemeBrush.IconBackgroundBrush}" HorizontalAlignment="Left" />
        <Grid Width="6" Background="{Binding ThemeBrush.IconBackgroundBrush}" HorizontalAlignment="Right" />
        -->
        <TextBlock Margin="10" Text="Window mode" Foreground="Gray" />
    </Grid>
</UserControl>
