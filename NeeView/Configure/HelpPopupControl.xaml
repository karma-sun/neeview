<UserControl x:Class="NeeView.Configure.HelpPopupControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeeView.Configure"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ToggleButton DockPanel.Dock="Right" x:Name="WindowTitleFormatHelp" Width="20" Height="20" Style="{StaticResource IconToggleButton}">
            <Image Width="16" Height="16" Source="{StaticResource ic_help_24px}"/>
        </ToggleButton>
        <Popup IsOpen="{Binding ElementName=WindowTitleFormatHelp, Path=IsChecked}" PlacementTarget="{Binding ElementName=WindowTitleFormatHelp}" Placement="Top" HorizontalAlignment="Right">
            <Border x:Name="PopupContentBorder" BorderBrush="Gray" BorderThickness="1" Background="{DynamicResource {x:Static SystemColors.InfoBrushKey}}" >
            </Border>
        </Popup>
    </Grid>
</UserControl>
