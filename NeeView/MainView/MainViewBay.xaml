<UserControl x:Class="NeeView.MainViewBay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeeView"
             mc:Ignorable="d"
             IsHitTestVisible="True"
             Background="{DynamicResource SideBar.Background}" Foreground="{DynamicResource SideBar.Foreground}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Margin="10" Text="Window mode" Foreground="{DynamicResource Control.GrayText}" />
    </Grid>
</UserControl>
