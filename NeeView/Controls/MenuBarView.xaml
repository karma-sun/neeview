<UserControl x:Class="NeeView.MenuBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeeView"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300">
    <UserControl.Resources>
        <local:PanelColorToBrushConverter x:Key="BackgroundSelector" Dark="{StaticResource NVMenuBackgroundDark}" Light="{StaticResource NVMenuBackgroundLight}"/>
        <local:PanelColorTobrushMultiConverter x:Key="ForegroundSelector" Dark="{StaticResource NVMenuForegroundDark}" Light="{StaticResource NVMenuForegroundLight}"/>
        <local:BooleanToOpacityConverter x:Key="BooleanToOpacityConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>


    <Grid x:Name="Root" KeyDown="Control_KeyDown_IgnoreSingleKeyGesture" Background="{Binding ThemeProfile.MenuColor, Converter={StaticResource BackgroundSelector}}" MinHeight="0">
        <DockPanel x:Name="MenuBarArea" LastChildFill="True" Margin="0,0,0,2">
            <local:WindowCaptionButtons x:Name="WindowCaptionButtons" VerticalAlignment="Top" DockPanel.Dock="Right" WindowChrome.IsHitTestVisibleInChrome="True">
                <local:WindowCaptionButtons.Foreground>
                    <MultiBinding Converter="{StaticResource ForegroundSelector}">
                        <Binding Path="ThemeProfile.MenuColor"/>
                        <Binding Path="IsActive" RelativeSource="{RelativeSource AncestorType=Window}"/>
                    </MultiBinding>
                </local:WindowCaptionButtons.Foreground>
                <local:WindowCaptionButtons.Style>
                    <Style TargetType="local:WindowCaptionButtons">
                        <Setter Property="Height" Value="28"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding WindowCaptionEmulator.IsEnabled}" Value="False">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Window.WindowState}" Value="Maximized">
                                <Setter Property="Height" Value="24"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </local:WindowCaptionButtons.Style>
            </local:WindowCaptionButtons>
            <Grid x:Name="Watermark" DockPanel.Dock="Right" Background="Orange" Margin="0" VerticalAlignment="Top" Visibility="Collapsed">
                <TextBlock x:Name="WatermarkText" Foreground="White" Margin="10,2" FontSize="12" FontWeight="Normal" Text="Canary"/>
            </Grid>

            <DockPanel x:Name="MenuArea" VerticalAlignment="Bottom">
                <ContentControl x:Name="MainMenuJoint" Content="{Binding MainMenu}" IsTabStop="False" VerticalAlignment="Center" WindowChrome.IsHitTestVisibleInChrome="True">
                    <ContentControl.Foreground>
                        <MultiBinding Converter="{StaticResource ForegroundSelector}">
                            <Binding Path="ThemeProfile.MenuColor"/>
                            <Binding Path="IsActive" RelativeSource="{RelativeSource AncestorType=Window}"/>
                        </MultiBinding>
                    </ContentControl.Foreground>
                </ContentControl>

                <TextBlock x:Name="MenuTitle" Text="{Binding WindowTitle.Title}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="50,1,0,1" TextTrimming="CharacterEllipsis"
                       FontSize="{x:Static SystemFonts.MessageFontSize}"
                       Visibility="{Binding WindowCaptionEmulator.IsEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock.Foreground>
                        <MultiBinding Converter="{StaticResource ForegroundSelector}">
                            <Binding Path="ThemeProfile.MenuColor"/>
                            <Binding Path="IsActive" RelativeSource="{RelativeSource AncestorType=Window}"/>
                        </MultiBinding>
                    </TextBlock.Foreground>
                </TextBlock>
            </DockPanel>

        </DockPanel>
    </Grid>

</UserControl>
