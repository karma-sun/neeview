<UserControl x:Class="NeeView.Setting.SettingItemSusiePluginControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:NeeView.Setting"
             xmlns:susie="clr-namespace:NeeView.Susie;assembly=NeeView.Susie"
             xmlns:nw="clr-namespace:NeeView.Windows"
             xmlns:properties="clr-namespace:NeeView.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>

        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <!--
        <CollectionViewSource x:Key="PluginSource" Source="{Binding SusiePluginList}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="ApiVersion" />
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
        -->

        <DataTemplate DataType="{x:Type susie:SusiePluginInfo}" x:Key="SusiePlugin">
            <StackPanel Orientation="Horizontal">
                <b:Interaction.Behaviors>
                    <nw:DragStartBehavior AllowedEffects="Move" DragDropFormat="SusiePlugin" DragDropData="{Binding}" Target="{Binding ElementName=PluginList}" />
                </b:Interaction.Behaviors>
                <CheckBox VerticalAlignment="Center" IsChecked="{Binding IsEnabled}" Checked="CheckBox_Changed" Unchecked="CheckBox_Changed"/>
                <Grid Width="20" VerticalAlignment="Center" Margin="2">
                    <Image Width="14" Height="14" Source="{StaticResource ic_setting_24px}" Visibility="{Binding HasConfigurationDlg, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                </Grid>
                <TextBlock Text="{Binding DetailText}" ToolTip="{Binding PluginVersion}" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                                    <Setter Property="Foreground" Value="Gray"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>               
        </DataTemplate>

        <Style TargetType="ListBox">
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
            <Setter Property="ItemTemplate" Value="{StaticResource SusiePlugin}"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="AllowDrop" Value="True"/>
            <EventSetter Event="PreviewDragOver"  Handler="PluginListView_PreviewDragOver"/>
            <EventSetter Event="Drop" Handler="PluginListView_Drop"/>
        </Style>


        <Style TargetType="CheckBox">
            <Setter Property="FocusVisualStyle" Value="{StaticResource NVBorderFocusVisual}" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Margin" Value="5,2"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="FocusVisualStyle" Value="{StaticResource NVBorderFocusVisual}" />
        </Style>

    </UserControl.Resources>

    <DockPanel x:Name="Root" Margin="0,0,5,5">

        <StackPanel DockPanel.Dock="Right">
            <StackPanel.Resources>
                <Style TargetType="StackPanel">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ElementName=PluginList, Path=SelectedItem}" Value="{x:Null}">
                            <Setter Property="IsEnabled" Value="False"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Resources>
            <Button x:Name="ConfigButton" Content="{x:Static properties:Resources.ControlSusiePluginListConfig}" Command="{Binding ConfigCommand}" TabIndex="2"/>
            <Button x:Name="UpButton" Content="{x:Static properties:Resources.ControlSusiePluginListUp}" Command="{Binding MoveUpCommand}" TabIndex="3"/>
            <Button x:Name="DownButton" Content="{x:Static properties:Resources.ControlSusiePluginListDown}" Command="{Binding MoveDownCommand}" TabIndex="4"/>
        </StackPanel>

        <ListBox x:Name="PluginList" MinHeight="64" MinWidth="64" SelectionChanged="PluginList_SelectionChanged" TabIndex="1">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <EventSetter Event="MouseDoubleClick" Handler="ListBoxItem_MouseDoubleClick"/>
                    <Setter Property="FocusVisualStyle" Value="{StaticResource NVFocusVisual}" />
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

    </DockPanel>

</UserControl>
