namespace NeeView
{
    public class BookmarkFolderListPresenter : FolderListPresenter
    {
        public BookmarkFolderListPresenter(BookmarkListView folderListView, FolderList folderList) : base(folderListView, folderList)
        {
        }
    }
}
