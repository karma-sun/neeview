<UserControl x:Class="NeeView.Setting.SettingItemCollectionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeeView.Setting"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <Grid x:Name="Root">
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Padding" Value="20,5"/>
                <Setter Property="Margin" Value="5"/>
            </Style>
        </Grid.Resources>
        <StackPanel>
        <ListBox x:Name="CollectionListBox" Height="128" ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Collection}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="AddButton" Content="Add..." Click="AddButton_Click" />
                <Button x:Name="RemoveButton" Content="Remove" Click="RemoveButton_Click">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=CollectionListBox, Path=SelectedItem}" Value="{x:Null}">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </StackPanel>

    </Grid>
</UserControl>
