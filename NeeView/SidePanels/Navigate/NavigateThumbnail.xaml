<UserControl x:Class="NeeView.NavigateThumbnail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeeView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityHiddenConverter" False="Hidden" />
    </UserControl.Resources>

    <Border x:Name="Root" BorderThickness="0" BorderBrush="DarkGray" Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityHiddenConverter}}">
        <Grid x:Name="ThumbnailGrid" Width="{Binding ThumbnailWidth}" Height="{Binding ThumbnailHeight}" Background="{Binding MainViewVisualBrush}" ClipToBounds="True" MouseMove="ThumbnailGrid_MouseMove">
            <Canvas>
                <Path Stroke="Red" StrokeThickness="1" Data="{Binding ViewboxGeometry}" RenderOptions.EdgeMode="Unspecified" />
            </Canvas>
        </Grid>
    </Border>
</UserControl>
