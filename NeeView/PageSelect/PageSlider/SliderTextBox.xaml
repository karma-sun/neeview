<UserControl x:Class="NeeView.SliderTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeeView"
             mc:Ignorable="d" 
             FontFamily="Arial"
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid x:Name="Root" MouseLeftButtonDown="SliderTextBox_MouseLeftButtonDown" IsHitTestVisible="True">
        <Grid.Resources>
            <local:SliderValueConverter x:Key="SliderValueConverter"/>
        </Grid.Resources>
        <Grid.Style>
            <Style TargetType="Grid">
                <Setter Property="Background" Value="Transparent"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#8888"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>
        <Grid>
            <TextBlock x:Name="TextBlock" TextAlignment="Center" VerticalAlignment="Center" Padding="8,0"
                   Text="{Binding DispText}">
            </TextBlock>
            <TextBox x:Name="TextBox"  TextAlignment="Center" VerticalAlignment="Center"
                 Text="{Binding Target.Value, UpdateSourceTrigger=Explicit, Converter={StaticResource SliderValueConverter}}"
                 Visibility="Hidden"
                 MouseWheel="TextBox_MouseWheel"
                 KeyDown="TextBox_PreviewKeyDown"
                 GotFocus="TextBox_GotFocus"
                 LostFocus="TextBox_LostFocus">
            </TextBox>
        </Grid>
    </Grid>

</UserControl>
