<UserControl x:Class="NeeView.DebugPageList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeeView"
             mc:Ignorable="d" 
             DockPanel.Dock="Right"
             d:DesignHeight="300" d:DesignWidth="600">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <ListView x:Name="Root" ItemsSource="{Binding BookOperation.PageList}"
              ScrollViewer.HorizontalScrollBarVisibility="Hidden"
              ScrollViewer.VerticalScrollBarVisibility="Visible">
        <ListView.View>
            <GridView>
                <GridViewColumn Header="Name"  DisplayMemberBinding="{Binding EntryLastName}" Width="80"/>
                <GridViewColumn Header="Condition"  DisplayMemberBinding="{Binding Content.DevStatus}" Width="80"/>
                <GridViewColumn Header="State"  DisplayMemberBinding="{Binding State}" Width="80"/>
                <GridViewColumn Header="Info"  DisplayMemberBinding="{Binding Message}" Width="150"/>
            </GridView>
        </ListView.View>
    </ListView>

</UserControl>
