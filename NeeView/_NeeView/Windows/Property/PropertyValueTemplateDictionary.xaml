<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:nlc="clr-namespace:NeeView.Windows.Controls"
                    xmlns:osc="clr-namespace:OpenSourceControls"
                    xmlns:local="clr-namespace:NeeView.Windows.Property">

    <DataTemplate DataType="{x:Type local:PropertyValue_Object}">
        <ContentControl Content="{Binding Value}" Foreground="Gray"/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:PropertyValue_Boolean}">
        <CheckBox IsChecked="{Binding Value}"/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:PropertyValue_String}">
        <nlc:TextBoxWithMessage Text="{Binding Value, Mode=TwoWay}" EmptyMessage="{Binding EmptyMessage}"/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:PropertyValue_Integer}">
        <TextBox Text="{Binding Value}"/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:PropertyValue_IntegerRange}">
        <DockPanel>
            <TextBox DockPanel.Dock="Right" Text="{Binding Value}" Width="70" VerticalAlignment="Center" Margin="5,0,0,0"/>
            <Slider Value="{Binding Value}" Minimum="{Binding Range.Minimum}" Maximum="{Binding Range.Maximum}" SmallChange="{Binding Range.SmallChange}" LargeChange="{Binding Range.LargeChange}" 
                        TickFrequency="{Binding Range.TickFrequency}" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
        </DockPanel>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:PropertyValue_Double}">
        <TextBox Text="{Binding Value}"/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:PropertyValue_DoubleRange}">
        <DockPanel>
            <TextBox DockPanel.Dock="Right" Text="{Binding Value}" Width="70" VerticalAlignment="Center" Margin="5,0,0,0"/>
            <Slider Value="{Binding Value}" Minimum="{Binding Range.Minimum}" Maximum="{Binding Range.Maximum}" SmallChange="{Binding Range.SmallChange}" LargeChange="{Binding Range.LargeChange}" 
                        TickFrequency="{Binding Range.TickFrequency}" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
        </DockPanel>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:PropertyValue_Percent}">
        <DockPanel>
            <TextBox DockPanel.Dock="Right" Text="{Binding Percent, StringFormat={}{0}%}" Width="70" VerticalAlignment="Center" Margin="5,0,0,0"/>
            <Slider Value="{Binding Percent}" Minimum="0" Maximum="100" SmallChange="5" LargeChange="20" 
                        TickFrequency="1" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
        </DockPanel>
    </DataTemplate>


    <DataTemplate DataType="{x:Type local:PropertyValue_Enum}">
        <ComboBox ItemsSource="{Binding Map}" SelectedValuePath="Key" DisplayMemberPath="Value" SelectedValue="{Binding SelectedValue}"/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:PropertyValue_Point}">
        <nlc:PointInspector Point="{Binding Value, Mode=TwoWay}"/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:PropertyValue_Size}">
        <nlc:SizeInspector Size="{Binding Value, Mode=TwoWay}"/>
    </DataTemplate>

    <!-- TODO: IsHsvModeの参照の一般化 -->
    <DataTemplate DataType="{x:Type local:PropertyValue_Color}">
        <nlc:ColorPicker Color="{Binding Value, Mode=TwoWay}" IsHsvMode="{Binding IsHsvMode, RelativeSource={RelativeSource AncestorType=UserControl,AncestorLevel=2}}"/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:PropertyValue_FilePath}">
        <nlc:FileNameBox Text="{Binding Value, Mode=TwoWay}" Filter="{Binding Filter}" FileDialogType="{Binding FileDialogType}" Note="{Binding Note}"/>
    </DataTemplate>


    <DataTemplate x:Key="PropertyValue_Boolean_ToggleSwitch">
        <nlc:ToggleSwitch IsChecked="{Binding Value}" Height="20" HorizontalAlignment="Right" VerticalAlignment="Center" Fill="{DynamicResource NVBackground}" Stroke="{DynamicResource NVForeground}" CheckedBrush="{DynamicResource NVButtonForeground}"/>
    </DataTemplate>

    <DataTemplate x:Key="PropertyValue_Color_ComboColorPicker">
        <DockPanel>
            <TextBox DockPanel.Dock="Right" Text="{Binding Value}" MinWidth="100" BorderThickness="1" VerticalContentAlignment="Center"/>
            <osc:ComboColorPicker SelectedColor="{Binding Value, Mode=TwoWay}"/>
        </DockPanel>
    </DataTemplate>

</ResourceDictionary>
